services:
  frontend:
    build: .
    container_name: hackathon-frontend
    expose:
      - "8082"
    networks:
      - infra-net

networks:
  infra-net:
    external: true
    name: hackathon_hackathon-net
