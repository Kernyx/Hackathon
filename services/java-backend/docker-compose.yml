version: '3.8'
services:
  java-backend:
    build: .
    container_name: hackathon-java
    expose:
      - "8080"
    env_file: ../../.env
    networks:
      - default
      - infra-net
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  infra-net:
    external: true
    name: hackathon_hackathon-net
